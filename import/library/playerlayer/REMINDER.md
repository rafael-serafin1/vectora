## HOW TO IMPORT:

<pre> 
    @vectora {
        import {
            playerlayer
        };
    }
</pre>

## HOW TO USE:

- nova propriedade:

- [ ] *sound*:

    - *play('caminho_do_arquivo', volume_entre_0_e_1)* ---> toca o som.

    - *loop('caminho_do_arquivo', volume_entre_0_e_1)* ---> toca o som em loop.

    - *pause(duração_)* ---> pausa o som por um certo período de tempo.

    - *stop(sem_parâmetro)* ---> para de tocar o som.
